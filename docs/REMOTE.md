# Repository Remote Connection

This project is now configured with a Git remote named `origin` pointing to:

```
https://github.com/isaacbuz/test.git
```

Use the following commands to verify or fetch from the remote:

```bash
git remote -v
git fetch origin
```
